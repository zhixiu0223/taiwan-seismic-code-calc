# 模型族譜——這個 repo 裡同時存在的幾條不同目的的模型線

之所以會覺得亂,是因為 repo 裡確實同時有三種不同角色的東西,職責
沒有完全切乾淨:

> **Stage = 設計主線;Case = 力學/方法驗證;VL = 診斷證據。**

一旦分清楚這三個角色,`equalDOF`、毛斷面、有效勁度、$\rho=2\%$、
`Fiber`、`Hinge`、真實配筋這些東西就不會互相打架。

## 模型族譜表

| 模型 | 目的 | 梁 | 柱 | 配筋 | 線性/非線性 | 是否設計需求來源 |
|---|---|---|---|---|---|---|
| `Case-04.5` | 真梁彈性驗證 | 彈性 | 彈性 | 簡化 | 線性 | ❌ |
| `Stage 2`(`Case-04.7`) | `Canonical demand` | 彈性 $I_e$ | 彈性 $I_e$ | 尚未設計 | 線性 | ✅ |
| `Stage 3` | `Demand extraction` | 同 `Stage 2` | 同 `Stage 2` | 尚未設計 | 線性 | ✅ |
| `Stage 4`(`case03_7`) | `RC design` | — | — | **正式設計** | — | 核心 |
| `Case-06` | 斷面機制驗證 | — | `Fiber` | 假設/研究 | 非線性 | ❌ |
| `Case-06.5 Hyb` | `model-form study` | 彈性 `0.35Ig` | `Fiber` | 假設 | 非線性 | ❌ |
| `Case-06.5 Fib` | `model-form study` | `Fiber` | `Fiber` | 假設 | 非線性 | ❌ |
| `Case-06.6` | `real-rebar nonlinear` | `Fiber` | `Fiber` | 實際配筋(`VL-14`) | 非線性 | 後續應改接 `Design Freeze` |

## 三種角色各自的判斷準則

- **`Stage`**(設計主線):目標是產出「這個 repo 正式採用的設計結果」,
  同一時間只有一條、不能互相矛盾,每個 `Stage` 都要餵給下一個 `Stage`
- **`Case`**(力學/方法驗證):目標是回答一個獨立的力學或方法論問題
  (「纖維斷面對不對?」「梁彈性假設有沒有支持?」),配筋/斷面可以是
  假設值,**不需要**跟 `Stage` 同步,也不該被要求同步
- **`VL`**(診斷證據):獨立的驗證記錄,通常是跨工具/跨方法比較,
  回答「這個特定的數字對不對」這種具體問題

## 常見疑問對照

| 疑問 | 答案 |
|---|---|
| 為什麼 `Case-06.5` 還在用 $\rho=0.02$,不是已經有真實配筋了嗎? | `Case-06.5` 是 `model-form study`(比較 `Fiber`/`Hinge`/`Hybrid` 這幾種理想化方式之間的差異),不是設計需求來源,配筋本來就該維持假設值當控制變因,不然沒辦法乾淨比較「模型形式」本身的影響 |
| `Stage 2` 為什麼不用剛性樓板隔板? | 這是待確認的建模假設邊界問題,不是絕對對錯——已知的做法是把它獨立成一個「敏感度研究」(`Model A` vs `Model B`),不回頭改已經四工具驗證過的版本 |
| `0.35Ig`/`0.7Ig` 是不是「真實的 `RC` 勁度」? | 不是,是這個分析目的下宣告的建模假設,不是唯一或絕對正確的值——`ACI` 本身的建議也隨分析目的而不同 |
