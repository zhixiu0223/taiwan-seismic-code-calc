# ROADMAP

本 repo 的定位:**回答「依照台灣耐震設計流程,可以完整重現並驗證嗎?」**

姐妹專案 [reproducible-structural-benchmarks](https://github.com/zhixiu0223/reproducible-structural-benchmarks)
定位不同、互不重疊:它回答「分析引擎(OpenSeesPy/FRAME3DD/CalculiX/suanPan)本身算得對不對」,
用的是已知答案的標竿案例(Ziemian & Ziemian 等),不涉及台灣法規。

兩個專案是**上下游關係**:
`reproducible-structural-benchmarks` 提供「這套分析工具可信」的基礎,
`taiwan-seismic-code-calc` 建立在這個基礎上,重現「建築資料 → 法規計算 → 建模 → 驗算」的完整流程。

---

## 核心原則:漸進式驗證,不一開始就衝大模型

> 越大的模型,越不知道錯在哪。

每一個 Case 都要能獨立驗證、獨立當作教學節點,而不是只有最後一個大模型。
新的 Case 只允許在前一個 Case **完全驗證通過**之後才開始,不允許同時展開多條戰線。

---

## Case 序列

```
Case-01  單自由度 (SDOF)
   │
   ▼
Case-02  一跨一層
   │
   ▼
Case-03  一跨二層
   │
   ▼
Case-03.5 試設斷面 (Trial Sizing)  ← 新增的必要前置步驟
   │
   ▼
Case-04  三跨二層(桃園案例,本 repo 目前進度)
   │
   ▼
Case-05  三維 RC(X向+Y向+樓板剛性+偏心)
   │
   ▼
Case-06  Pushover + FEMA 273 雙直線簡化
   │
   ▼
Case-07  反應譜分析 + ATC-40 容量光譜法(CSM)+ SRSS/CQC
   │
   ▼
Case-08  時程分析
```

### Case-01:單自由度(SDOF)
- 目的:驗證最基礎的動力學關係(F=Ku、T=2π√(m/K))
- 可完全手算,是後面所有 Case 的公式基礎
- **[已完成] notebooks/case01_sdof.ipynb —— 特徵值分析週期與靜力位移皆與手算閉合解完全吻合(相對誤差 0.00e+00)**

### Case-02:一跨一層
- 第一次接觸 OpenSeesPy 的節點/元素/邊界條件定義
- 幾何最簡單,任何錯誤一眼看得出來

### Case-03:一跨二層
- 引入樓層數,開始出現「層剪力分配」的概念
- 仍然全部可手算對照

### Case-03.5:試設斷面(Trial Sizing)——新增的必要前置步驟
- **這一步之前,手上只有地震力需求(V、Fx),沒有任何梁柱斷面**
- 等值靜力法算出的是「需求」,不是「斷面」;OpenSeesPy 的
  `elasticBeamColumn` 需要 A、I,必須先有一組試設斷面才能建模
- 流程:概略試設(用彎矩需求反推最小斷面,梁用跨度/深度經驗比
  L/10~L/12)→ 建模分析 → 檢核強度與位移(規範 0.005)→
  不夠就放大斷面重跑
- 這個「試設→分析→檢核→調整」的迴圈本身就是結構設計的常態,
  在正式進入 Case-04 前先在最簡幾何上跑過一次,確保迴圈本身沒問題

### Case-04:三跨二層(桃園案例)——本 repo 目前進度
- 對應 2 層樓 8 柱 RC 構架(桃園市桃園區)
- 目前已完成:法規查表(SDS/SD1/Fu/Ra)→ 基底剪力 V →
  樓層地震力分配(F1/F2)→ 反曲點法概估柱端彎矩
- 尚待:X 向 3 跨構架 + Y 向 1 跨構架,各自建 2D OpenSeesPy 模型,
  代入試設斷面,驗證真實位移與柱剪力分配(而非反曲點法「等剛度均分」假設)

### Case-05:三維 RC
- 加入 X 方向、Y 方向、樓板剛性假設、質量偏心(含規範規定的 5% 意外偏心)
- 到這一步才是接近真實工程送審等級的模型
- 前提:Case-04 的兩個 2D 方向模型都已驗證過,才展開三維整合

### Case-06:Pushover + FEMA 273 雙直線簡化
- 引入 Fiber Section(Concrete01、Steel02)取代彈性梁柱元素
- 跑側推分析得到桃園案例**真實的** Pushover 曲線(非課程示範數字)
- 套 FEMA 273 等面積雙直線法,算出這個案例真正的 Vy、Keff、Teff

### Case-07:反應譜分析 + ATC-40 CSM
- 用 Case-06 的 Pushover 曲線轉換到 ADRS 座標(Sa-Sd)
- 疊上桃園案例真實的需求譜(SDS=0.55g、SD1=0.45g,非課程示範值)找性能點
- 同時實作多振態組合:SRSS 與 CQC 對比,驗證本案例週期分佈下
  兩者差異是否顯著(判斷簡化法是否足夠)

### Case-08:時程分析
- 加入實際地震歷時,驗證等值靜力法與反應譜法的簡化誤差有多大
- 屬於額外的方法論交叉驗證層,非規範強制(規範僅要求
  高度≥50m 或樓層≥15層才強制動力分析),本案例遠低於門檻

---

## FEMA 273 / ATC-40 的定位澄清

這兩份文件是美國既有建築耐震評估/補強指引(FEMA 273 已被 ASCE 41 取代),
**不是台灣新建物法定合規流程的一部分**。放進本 repo 的理由是:
Case-06/07 需要一套建立 Pushover→性能點的既有方法論當骨架,
FEMA 273 的雙直線簡化與 ATC-40 的容量光譜法剛好是這個骨架最成熟的既有做法。

之前產出的「結構動力與耐震性能設計」課程講義範例(SDOF/MDOF、Bilinear 簡化、
ATC-40 CSM)用的是**課程示範數字**,不是桃園案例的真實數字,不能直接視為
Case-06/07 的成果——Case-06/07 必須用桃園案例自己跑出來的 Pushover 曲線
重新代入這套方法論,才算完成。

---

## 明確不做的事(避免範圍蔓延)

- 不在 Case 完全通過前平行展開下一個 Case
- Case-05 之前不加樓板剛性以外的三維複雜度(不加牆、不加地下室)
- 不为了「更真實」而跳過中間 Case 直接做大模型
